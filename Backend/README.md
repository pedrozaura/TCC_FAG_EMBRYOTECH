```bash
Comando para executar o Backend

flask run --host=0.0.0.0 --port=5001

```

docker compose build
559 docker compose up -d
560 dockwer ps
561 docker ps
562 docker exec -it f00f93c54fd7 bash

compilar o docker sem cache

docker-compose -build --no-cache
